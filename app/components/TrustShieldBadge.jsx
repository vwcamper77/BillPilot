import TrustShield from "@/components/TrustShield";

export default function TrustShieldBadge({ className = "", compact = false }) {
  return <TrustShield className={className} compact={compact} />;
}
